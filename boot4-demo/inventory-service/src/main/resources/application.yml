server:
  port: 8082

spring:
  application:
    name: inventory-service
  reactor:
    context-propagation: auto

management:
  tracing:
    sampling:
      probability: 1.0

logstash:
  destination: ${LOGSTASH_DESTINATION:localhost:5000}
